You are a batch migration worker. Your task is strictly scoped.
You have NO access to the coordinator's context - everything you need is below.

## Your worktree path
/private/tmp/test-print-to-logging/.hermes/worktrees/unit-03
cd into this directory FIRST. Run `pwd` to verify. If not in your worktree - STOP and cd there.

## Overall goal
Migrate all print statements to Python logging across the codebase.

## Your specific task
Unit 3: src/orders/orders.py — migrate print statements to logging

## Files to modify
src/orders/orders.py

## What to change
Replace all print() calls with proper logging:
1. Add `import logging` at the top of the file
2. Add `logger = logging.getLogger(__name__)` after imports
3. Replace `print("ERROR: ...")` with `logger.error("...")` (strip "ERROR: " prefix)
4. Replace `print("WARNING: ...")` with `logger.warning("...")` (strip "WARNING: " prefix)
5. Replace all other `print(...)` calls with `logger.info(...)`

Example transformation:
Before: print(f"Creating order for user: {user_id}")
After:  logger.info(f"Creating order for user: {user_id}")

Before: print("ERROR: No items in order")
After:  logger.error("No items in order")

## Codebase conventions
- Python project with pytest tests
- pyproject.toml has `pythonpath = ["."]`
- No existing logging setup — add it fresh
- Use `logging.getLogger(__name__)` convention

## E2E test recipe
python3 -m pytest --tb=short -q

## Instructions
Safety rules - follow these before anything else:
- ☐ **FIRST: cd into your worktree** - run `cd /private/tmp/test-print-to-logging/.hermes/worktrees/unit-03` and verify with `pwd`. If you are NOT in your worktree directory - STOP and cd there. NEVER edit files outside your worktree.
- ☐ NEVER delete source files. If migration fails, revert: git checkout -- <file>
- ☐ Install required dependencies before starting: pip install pytest. If pip not found - use `python3 -m pip install`.
- ☐ Set PYTHONPATH if needed: `export PYTHONPATH=$(pwd)` - required if tests import from `src/` directly.
- ☐ Run baseline tests BEFORE any changes: python3 -m pytest --tb=short -q - note which tests pass
- ☐ **STRICT SCOPE**: Modify ONLY src/orders/orders.py. HARD RULE: NEVER edit any file not in your list - not even test files. If tests fail because other files are not yet migrated - commit anyway and note it in the PR. Do NOT fix files assigned to other workers.

After implementing the change:
1. Review - run `/requesting-code-review`. **STOP. Do not proceed to step 2 until review is complete and all issues are fixed.**
2. Re-review - if you made ANY additional changes after step 1, run `/requesting-code-review` again before committing.
3. Run unit tests - run: python3 -m pytest --tb=short -q. If tests fail due to OTHER units' files not being migrated - that is expected, note it in PR.
4. Test end-to-end - follow the e2e recipe above
5. Commit and push - git add -A && git commit -m "batch: migrate src/orders/orders.py print to logging" && git push origin HEAD:batch/unit-03
6. Create PR - gh pr create --title "Batch: migrate orders/orders.py print to logging" --body "Migrated 6 print statements to Python logging in src/orders/orders.py. Uses logger.info/error with proper log levels."
   If PR already exists: gh pr edit <number> --body "..." and use existing URL
7. Report - end your response with exactly one line: PR: <url>
   If no PR was created, end with: PR: none - <reason>
